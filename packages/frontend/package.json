{
  "name": "@kas/frontend",
  "private": true,
  "version": "0.0.1",
  "description": "Stencil Frontend",
  "scripts": {
    "build": "npx stencil build",
    "dev": "npx stencil build --dev --watch",
    "serve": "npx stencil build --dev --watch --serve",
    "test": "npx stencil test --spec --e2e",
    "test.watch": "npx stencil test --spec --e2e --watchAll",
    "generate": "npx stencil generate"
  },
  "dependencies": {
    "@stencil/core": "^1.3.3",
    "@stencil/router": "^1.0.1",
    "@kas/shared": "^0"
  }
}
